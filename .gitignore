.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.DS_Store
frontend/node_modules/
frontend/dist/
.runtime/

# Build-only feature cache; runtime artwork, masks, metadata, and similarity
# data are intentionally committed so Streamlit Cloud can start without a build step.
/data/features/*.json

# Interrupted similarity builds resume from this transient state.
*.checkpoint.json
